[build]
  functions = "functions/"  # Keep this if you have functions
  publish = "."            # Publish from the root directory
  command = "npm run build"  # Keep this if you have a build command
